import VantaNetBackground from "./VantaNetBackground";

/**
 * Global background layer — clean, cohesive Vanta.NET canvas matching the design system.
 */
export default function Background() {
  return <VantaNetBackground />;
}